一、样式定制
_bootstrap-config.scss：配置bootstrap中各组件的变量


二、使用Sass的目的
1.复用
	$变量: 属性值;
	@mixin 混合器(形参);  ...  @include 混合器(实参);
	@extend 其它选择器;

2.增加可维护性
	嵌套写法：结构清晰
	文件导入：划分组件
	注释灵活：可留可删

3.灵活性
	支持运算：属性值
	提供颜色函数库